spring:
  application:
    name: gateway-route-operator
  main:
    web-application-type: none
    keep-alive: true
  threads:
    virtual:
      enabled: true

operator:
  namespace: platform
  reconcile:
    debounce: 200ms
  routes:
    store:
      type: configmap
      configmap:
        namespace: platform
        config-map-name: gateway-routes
        key: routes.yaml
      s3:
        bucket: ""
        key: ""
        region: us-east-1

logging:
  level:
    com.example.operator: INFO
    io.fabric8.kubernetes.client: WARN